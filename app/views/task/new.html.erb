<div class="container">
  <h1> Add a new task </h1>
  <%= simple_form_for(@task) do |f| %>
  <%= f.input :title %>
  <%= f.input :details %>
  <%= f.submit %>
  <% end %>
  <p><%= link_to "Back to list", tasks_path(@task) %></p>
</div>

